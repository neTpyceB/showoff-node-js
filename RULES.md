# Rules

- Keep implementation limited to the documented routes and runtime roles.
- Use only the current runtime and tool versions defined in this repository.
- Keep runtime code minimal and avoid extra logic outside caching, balancing, metrics, logs, and health checks.
- Update docs, Docker, tests, and CI together with code changes.
