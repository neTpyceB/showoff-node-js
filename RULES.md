# Rules

- Keep implementation limited to the documented event bus, worker, and query routes.
- Use only the current runtime and tool versions defined in this repository.
- Keep runtime code minimal and avoid extra logic outside publishing, projections, health, logging, and retry.
- Update docs, Docker, tests, and CI together with code changes.
