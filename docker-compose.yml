services:
  api:
    build:
      context: .
      target: runtime
    image: simple-rest-api:runtime
    ports:
      - "3000:3000"
  test:
    build:
      context: .
      target: test
    image: simple-rest-api:test
    profiles:
      - test
