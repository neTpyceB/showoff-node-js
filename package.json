{
  "name": "realtime-chat-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal realtime chat backend with rooms, presence, and persisted messages.",
  "type": "module",
  "engines": {
    "node": ">=24.14.0 <25"
  },
  "scripts": {
    "migrate": "node scripts/migrate.js",
    "start": "npm run migrate && node src/server.js",
    "lint": "eslint .",
    "test:unit": "node --test \"test/unit/**/*.test.js\"",
    "test:integration": "node --test \"test/integration/**/*.test.js\"",
    "test:e2e": "node --test test/e2e/service.test.js",
    "test:smoke": "node --test test/smoke/service.test.js",
    "test": "npm run test:unit && npm run test:integration && npm run test:e2e && npm run test:smoke",
    "coverage": "c8 --100 npm run test",
    "audit": "npm audit --audit-level=low",
    "check": "npm run migrate && npm run lint && npm run coverage && npm run audit",
    "check:container": "npm run migrate && npm run lint && npm run coverage",
    "smoke:ws": "node scripts/request-smoke.js"
  },
  "dependencies": {
    "better-sqlite3": "12.8.0",
    "ws": "8.20.0"
  },
  "devDependencies": {
    "c8": "11.0.0",
    "eslint": "10.1.0"
  }
}
